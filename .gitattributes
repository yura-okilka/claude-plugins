# Normalize line endings; let Git decide per file by default.
* text=auto

# Windows scripts must keep CRLF in the working tree.
*.ps1 text eol=crlf
*.vbs text eol=crlf

# Binary assets — never apply EOL or text filters.
*.png binary
